# Issue Tracker

Issues are tracked in **GitHub Issues** at `cowboydiver/timber-compare`.

Use the `gh` CLI for all issue operations:

- Create: `gh issue create --title "..." --body "..."`
- Read: `gh issue view <number>`
- List: `gh issue list`
- Label: `gh issue edit <number> --add-label "..."`
- Close: `gh issue close <number>`

## Repository

`cowboydiver/timber-compare`
